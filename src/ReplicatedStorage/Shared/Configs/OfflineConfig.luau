--!strict
-- OfflineConfig — Faza 3, OfflineEarnService. Flat-rate Esencji za czas offline, PORT wzorca z
-- D:/RobloxProjects/rng EconomyService.ComputeOfflineEarnings (3 guardy: first-login, clock-skew,
-- cap). Wartość PLACEHOLDER — tuning po playteście, jak EssenceConfig.RunGoldToEssenceRate.

local OfflineConfig = {}

OfflineConfig.EssencePerHour = 20
OfflineConfig.CapHours = 10

return OfflineConfig
